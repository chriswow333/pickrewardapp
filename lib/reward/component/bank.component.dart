import 'package:flutter/material.dart';

class BanksComponent extends StatelessWidget {
  const BanksComponent({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child:const Row(
        children:[
          
        ]
      )
    );
  }
}